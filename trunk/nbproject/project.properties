application.args=
application.desc=Remote Service Configurator
application.homepage=rsc.berlios.de
application.title=rsc
application.vendor=wariin
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/rsc.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
includes=**
jar.compress=false
javac.classpath=\
    ${libs.swing-layout.classpath}:\
    ${libs.absolutelayout.classpath}:\
    ${libs.jsch.classpath}:\
    ${libs.jdom.classpath}:\
    ${libs.commons-vfs.classpath}:\
    ${libs.l2fprod.classpath}:\
    ${libs.commons-logging.classpath}:\
    ${libs.antlr.classpath}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:deprecation
javac.deprecation=false
javac.source=1.5
javac.target=1.6
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
jnlp.codebase.type=local
jnlp.codebase.url=file:/home/wariin/dev/rsc/dist/
jnlp.enabled=false
jnlp.offline-allowed=false
jnlp.signed=false
# Property libs.antlr.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.antlr.classpath=../javalibs/antlr-3.1.jar
# Property libs.commons-lang.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.commons-lang.classpath=../javalibs/commons-lang-2.4/commons-lang-2.4.jar
# Property libs.commons-logging.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.commons-logging.classpath=../javalibs/commons-logging-1.1.1/commons-logging-1.1.1.jar:../javalibs/commons-logging-1.1.1/commons-logging-adapters-1.1.1.jar:../javalibs/commons-logging-1.1.1/commons-logging-api-1.1.1.jar
# Property libs.commons-vfs.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.commons-vfs.classpath=../javalibs/commons-vfs-1.0/commons-vfs-1.0.jar
# Property libs.jakarta-oro.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.jakarta-oro.classpath=../javalibs/jakarta-oro-2.0.8/jakarta-oro-2.0.8.jar
# Property libs.jakarta-regexp.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.jakarta-regexp.classpath=../javalibs/jakarta-regexp-1.5/jakarta-regexp-1.5.jar
# Property libs.jdom.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.jdom.classpath=../../../../home/mariin/javalibs/jdom-1.0/build/jdom.jar
# Property libs.jregex.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.jregex.classpath=../javalibs/jregex1.2_01.jar
# Property libs.jsch.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.jsch.classpath=../jsch.jar
# Property libs.l2fprod.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.l2fprod.classpath=../javalibs/l2fprod-common-7.3/lib/l2fprod-common-all.jar
# Property libs.liquid.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.liquid.classpath=../liquidlnf.jar
main.class=rsc.RSC
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=src
test.src.dir=test
